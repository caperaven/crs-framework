import "./../../components/maked-input/masked-input.js"

export default class Form extends crsbinding.classes.ViewBase {
    async connectedCallback() {
        await super.connectedCallback();
    }
}