import "./../../components/text-editor/text-editor.js";

export default class TextEditor extends crs.classes.ViewBase {
    async connectedCallback() {
        await super.connectedCallback();
    }
}